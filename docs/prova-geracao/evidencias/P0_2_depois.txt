DEPOIS (usa o item do loop 'problema'):

        for problema in _as_list(input_data.get('problemas')):
            cur.execute('INSERT INTO problemas (persona_id, descricao) VALUES (%s, %s)', [input_data.get('persona_id'), problema])
        _result = {'status': 'sucesso'}

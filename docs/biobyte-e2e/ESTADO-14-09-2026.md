# BioByte Sentinela — estado medido em 14/09/2026

## O que está provado rodando

- **Geração APROVADA nos cinco portões** (sessão c88c5299): lógica 0, ferramentas 0, contrato de
  tela 0, módulos 0, rede de fluxo 0. 105 arquivos.
- **Implantação sobe sozinha**, sem forçar, e agora **prepara o banco**: criou as 14 tabelas do
  modelo atual no `biobyte_app` (as 8 antigas, no plural, foram mantidas — não apago dado).
- **Telas de cadastro mostram dado real**: "1 de 1 registro", caso ATIVO, com ver/editar/excluir.
- **6 das 7 tarefas da cadeia clínica executam de ponta a ponta** contra banco e serviços externos:

| tarefa | resultado medido |
|---|---|
| view_surveillance_dashboard | sucesso — casos_ativos: 1 |
| detect_mdr (sem antibiograma) | sucesso — multirresistente: **false** (não inventa) |
| detect_mdr (com 4 classes resistentes) | **detectou** e encadeou a notificação |
| calculate_cox_score | sucesso — escore 0,1556, faixa "Baixo", via serviço externo MCP |
| classify_case_nhsn | sucesso — CONFIRMADO, critério ICSAC, versão 2024 |
| recommend_treatment_bundle | sucesso — bundle recomendado com justificativa citando o NHSN |
| estimate_risk_reduction | **falha** no serviço externo (pendência) |

- **O sistema recusa em voz alta, sem mock**: a notificação parou com "SMTP não configurado.
  Defina SMTP_HOST/SMTP_PORT/… Sem mock."; a recomendação recusou com "sem_classificacao_nhsn"
  (regra de negócio) até o caso ser classificado.

## Defeitos encontrados POR RODAR (todos registrados, nenhum escondido)

1. **Coluna curta demais para o que o serviço externo devolve**: `escore_risco.versao_modelo` tinha
   20 caracteres e o serviço devolve "Cox proportional hazards" (24). O modelo de dados dimensionou
   sem saber o que a integração entrega.
2. **Campo obrigatório que a integração não fornece**: `escore_risco.intervalo_confianca` é NOT NULL
   e o serviço externo não devolve intervalo de confiança.
3. **Colunas de auditoria sem valor padrão**: `log_auditoria.hash_anterior` e `hash_atual` sem
   default, e um dos caminhos de gravação não os informa.
4. **Tabela de tratamento com seis colunas obrigatórias sem padrão**, incluindo a própria redução de
   risco, que só existe depois do cálculo.
5. **Mesmo dado com dois nomes**: `usuario_id` e `id_usuario`, `periodo` e `periodo_dias`,
   `id_caso` e `caso_id` — tarefas diferentes pedem grafias diferentes da mesma coisa.
6. **Campos opcionais tratados como obrigatórios**: a classificação exige `classificacao_sobrescrita`
   e `observacao_medico` mesmo quando não há sobrescrita.
7. **A sessão não chega às telas**: o painel recusa com "usuario_id ausente" porque a tela não
   carrega quem está logado. É o mesmo elo que a revisão da Rede de Petri apontou.
8. **A mensagem de erro da tela de login aparece truncada** (mostra uma letra).

Os itens 1 a 4 foram ajustados no banco para destravar a demonstração; **o conserto de verdade é no
gerador**, no dimensionamento do modelo de dados a partir do que as integrações devolvem.

# BioByte Sentinela — Registro de narração + produção (fonte do vídeo)

Projeto: **BioByte Sentinela** (ICSAC) · id `bab9d113-eff1-474f-8acc-0abfa516cd7d` · domínio Saúde · framework crewai · protocolo okf.
Ambiente: frontend :3005 → backend :8003 → banco local 127.0.0.1:3308 + qwen3.8-27b local. Telas em `shots/`.

Formato por cena: **NARRAÇÃO** (fala exata p/ locução) · **PRODUÇÃO** (o que mostrar/destacar) · **TELA(s)**.

---

## Cena 1 — Abertura: o que é a ferramenta
**NARRAÇÃO:** Esta é a LangNet — a ferramenta que implementa o Desenvolvimento Orientado a Especificação, o S D D que apresentamos. A ideia é simples e poderosa: em vez de escrever código direto, você constrói uma cadeia de artefatos — requisitos, especificação, modelo de dados, protótipo, agentes, testes — e o código nasce dessa cadeia, rastreável de ponta a ponta. Vamos ver isso na prática construindo um sistema real de vigilância de infecção hospitalar.
**PRODUÇÃO:** cartão de título "LangNet · SDD"; ao fundo, a tela de Projetos. Segurar 4s.
**TELA:** 00_projetos.png

## Cena 2 — Criando o projeto
**NARRAÇÃO:** Tudo começa criando o projeto. Chamamos de BioByte Sentinela, domínio Saúde. Repare nas opções: o framework de agentes — o nosso padrão é o CrewAI —, e o protocolo de interoperabilidade — o nosso é o O K F. Ficamos com os padrões, que são os que rodam no nosso pipeline.
**PRODUÇÃO:** modal "Criar Novo Projeto"; destacar em zoom o Nome, o Domínio "Saúde", e em Opções Avançadas os seletores **Framework: CrewAI (padrão)** e **Protocolo: OKF — padrão (nosso)**. Mostrar as listas de opções (LangChain/LangGraph/AutoGen/OpenAI SDK/Anthropic SDK; MCP/A2A/ACP/ANP).
**TELA:** 01_criar_modal.png, 01b_criar_preenchido.png
**TRECHO (opções, exibir):**
```
Framework: CrewAI (padrão) · LangChain · LangGraph · AutoGen · OpenAI SDK · Anthropic SDK
Protocolo: OKF — padrão (nosso) · MCP · A2A · ACP · ANP  (mock por ora)
```

## Cena 3 — Projeto criado, o menu do pipeline
**NARRAÇÃO:** Criado o projeto, o menu lateral revela o pipeline inteiro do S D D: Documentos, Especificação, Modelo de Dados, Interface e Protótipo, Agentes e Tarefas, o YAML, a Sequência de Tarefas, a Rede de Petri, a Geração de Código e os Casos de Teste. Vamos passar por cada etapa, e mostrar como cada uma se liga à anterior.
**PRODUÇÃO:** clicar "Abrir Projeto"; mostrar a barra lateral com as etapas. (captura a seguir)
**TELA:** 02_pos_criacao.png (+ sidebar a capturar)

---
<!-- as próximas cenas (Documentos/Requisitos, Especificação, ...) serão anexadas conforme cada etapa for gerada e validada -->

## Cena 4 — Etapa 1: Documentos (a origem)
**NARRAÇÃO:** A primeira etapa do pipeline é Documentos. Aqui trazemos os arquivos de origem — no nosso caso, o resumo do problema de vigilância de infecção por cateter. À esquerda ficam os documentos e o painel de configuração; escrevemos as instruções de análise, e clicamos em Iniciar Análise. É neste ponto que a ferramenta lê a fonte e gera, sozinha, o documento de Requisitos estruturado.
**PRODUÇÃO:** tela Documentos com o arquivo `brief_icsac.md` na lista (status Pendente), o campo "Instruções para Análise" preenchido, e o botão "🚀 Iniciar Análise". Destaque o clique em Iniciar Análise e a barra/mensagem "Iniciando análise…".
**TELA:** 10_doc_landing.png, 11_doc_upload_modal.png, 12_doc_uploaded.png, 13_doc_pronto_analise.png, 14_analise_iniciada.png
**NOTA PRODUÇÃO:** o processamento leva alguns minutos (qwen local); no vídeo, cortar para o resultado (não mostrar a espera).

## Cena 5 — O documento de Requisitos gerado (mostrar as partes)
**NARRAÇÃO:** E este é o documento de Requisitos que a ferramenta gerou, sozinha, a partir do brief. Ele tem três camadas, e vale entender cada uma. Primeiro, os **requisitos funcionais** — o que o sistema deve fazer: calcular o escore de risco pelo modelo de Cox, importar a microbiologia, classificar o caso pelo critério N H S N, detectar multirresistência e recomendar o bundle. Depois, os **requisitos não-funcionais** — as metas de qualidade: o cálculo do escore em menos de dois segundos, criptografia dos dados de saúde, conformidade com o N H S N e alta disponibilidade. E, por fim, as **regras de negócio** — as políticas que governam as decisões: por exemplo, se um microrganismo multirresistente é detectado, gerar um alerta; e reter os logs de auditoria por cinco anos. Repare que cada requisito já traz suas dependências — a rastreabilidade começa aqui.
**PRODUÇÃO:** abrir o documento de Requisitos (👁 Visualizar) e rolar mostrando, em sequência e com um rótulo em zoom em cada uma:
  1) tabela **Requisitos Funcionais** (destacar FR-001 Escore de Risco/Cox, FR-003 Classificação NHSN, FR-005 Bundle) — rótulo "REQUISITOS FUNCIONAIS = o que o sistema faz";
  2) tabela **Requisitos Não-Funcionais** (NFR-001 Cox <2s, NFR-003 AES-256/TLS 1.3) — rótulo "NÃO-FUNCIONAIS = metas de qualidade";
  3) tabela **Regras de Negócio** (BR-002 multirresistência→alerta, BR-005 logs 5 anos) — rótulo "REGRAS DE NEGÓCIO = políticas de decisão".
  Mostrar no canto o "Document Stats" (972 linhas, 22 seções) como prova de que é um documento completo.
**TELA:** 15_req_topo.png, 16_req_fr.png, 17_req_nfr.png, 18_req_br.png
**NOTA PRODUÇÃO:** manter curto (~25-30s de narração); não ler as tabelas inteiras, só apontar 2-3 itens de cada e o rótulo do tipo.

## Cena 6 — Etapa 2: Especificação (deriva dos Requisitos)
**NARRAÇÃO:** A segunda etapa é a Especificação Funcional. E repare num detalhe que é o coração do S D D: para gerar a especificação, a ferramenta pede que você escolha, explicitamente, qual documento de Requisitos — e qual versão — vai alimentá-la. Ou seja, a especificação nasce amarrada a uma versão exata dos requisitos. Selecionamos a versão 1 dos nossos Requisitos, escrevemos as instruções pedindo casos de uso com fluxos completos e a matriz de rastreabilidade, e mandamos gerar.
**PRODUÇÃO:** mostrar o clique em "📋 Requisitos" → o modal "Selecionar Documento de Requisitos" → a sessão "Document Analysis" → "Versão 1" → o preview → botão verde "✅ Selecionar este Documento". Depois o botão "🚀 Gerar Especificação" ficando habilitado. Enfatizar a frase: "a spec fica presa à versão dos requisitos".
**TELA:** 20_spec_landing.png, 20c_spec_versoes.png, 20d_spec_req_ok.png, 21_spec_pronto.png, 22_spec_gerando.png
**NOTA PRODUÇÃO:** a geração leva minutos (qwen local) — cortar para o resultado. O croqui dos casos de uso está em modo ASCII (opção "Wireframe dos Casos de Uso").

## Cena 7 — A Especificação gerada (mostrar as partes)
**NARRAÇÃO:** E aqui está a Especificação Funcional que a ferramenta derivou dos Requisitos — um documento de catorze seções. Vale abrir e entender do que ele é feito. Logo no início, a seção 3 lista os **requisitos cobertos**: cada requisito funcional, não-funcional e cada regra de negócio que esta especificação promete realizar — é a primeira amarração com a etapa anterior. No coração do documento estão os **Casos de Uso**: doze deles, e cada caso de uso traz o **fluxo principal**, os **fluxos alternativos** e os **fluxos de exceção**. Veja o caso de uso seis, Calcular Escore de Risco pelo modelo de Cox: no fluxo principal, o médico clica em "Calcular Escore de Risco" e o sistema chama o serviço externo de Cox; no alternativo, faltam parâmetros clínicos; na exceção, o serviço de Cox dá timeout. E cada caso de uso termina com o seu **croqui de tela** — o wireframe da interface.
**PRODUÇÃO:** abrir 👁️ Visualizar Documento e rolar mostrando, com rótulo em zoom:
  1) topo do documento — "Especificação Funcional… Baseado em: Requisitos v1" (rótulo "DERIVA DOS REQUISITOS v1");
  2) seção **3.1 Requisitos Funcionais Cobertos** e **3.3 Regras de Negócio Cobertas** (rótulo "O QUE ESTA SPEC REALIZA");
  3) **UC-006** com as três tabelas de fluxo — Principal / Alternativos / Exceção (rótulo "TODO CASO DE USO: principal + alternativo + exceção");
  4) o **Wireframe da Interface** do UC-006 (croqui ASCII).
**TELA:** 23_spec_doc_topo.png, 24_spec_sec3_fr.png, 24b_spec_sec3_br.png, 25_spec_uc006_topo.png, 25b_spec_uc006_fluxos.png, 26_spec_uc006_wireframe.png
**NOTA PRODUÇÃO:** curto (~30s). Não ler as tabelas; apontar 2-3 itens e o rótulo do tipo.

## Cena 8 — Rastreabilidade: a matriz que liga tudo
**NARRAÇÃO:** Mas o ponto que a nossa palestra mais enfatiza é a **rastreabilidade** — e a ferramenta a torna explícita numa **matriz**, na seção treze. Cada linha pega um requisito original e mostra em qual seção da especificação ele foi tratado, qual caso de uso o realiza e qual regra de negócio o governa. O requisito funcional um — o escore de Cox — é realizado pelo caso de uso seis e regido pela regra de negócio três. O requisito quatro — multirresistência — pelo caso de uso cinco. Os catorze requisitos funcionais, os sete não-funcionais e as cinco regras de negócio: todos rastreados, do requisito ao caso de uso. É essa cadeia que sobrevive por todo o pipeline.
**PRODUÇÃO:** rolar até **13.1 Matriz de Rastreabilidade**; destacar as linhas FR-001→UC-006→RN-003 e FR-004→UC-005→RN-002; rótulo "RASTREABILIDADE = requisito → seção → caso de uso → regra".
**TELA:** 27_spec_matriz.png

## Cena 9 — Validação crítica: achamos um problema no croqui
**NARRAÇÃO:** Documento gerado não é documento aprovado. A gente valida. E ao conferir o caso de uso seis, aparece uma inconsistência real entre o **fluxo** e o **croqui**. No fluxo principal, o médico age na tela "Detalhe do Caso" e clica no botão "Calcular Escore de Risco", e o sistema usa os parâmetros idade, APACHE Dois e tipo de cateter. Mas o croqui desenhou outra tela — "Painel de Prognóstico" —, com um botão diferente, "Recalcular", e esqueceu o APACHE Dois na lista de fatores. Fluxo e wireframe têm que casar: mesma tela, mesmo botão, mesmos campos. Achamos o problema — agora vamos corrigir do jeito certo: pedindo ao próprio agente.
**PRODUÇÃO:** tela dividida — à esquerda 25b (fluxo do UC-006, destacar "Detalhe do Caso" / "Calcular Escore de Risco" / "APACHE II"), à direita 26 (wireframe, destacar "Painel de Prognóstico" / "Recalcular"). Marcar em vermelho os três pontos divergentes.
**TELA:** 25b_spec_uc006_fluxos.png, 26_spec_uc006_wireframe.png

## Cena 10 — Correção pelo agente + as duas versões lado a lado
**NARRAÇÃO:** A correção não é na mão — é conversando com o agente da especificação. Escrevemos a instrução: no caso de uso seis, alinhe o croqui ao fluxo — tela "Detalhe do Caso", botão "Calcular Escore de Risco", e inclua o APACHE Dois nos fatores de risco. Clicamos em Refinar. O agente gera uma **versão dois** do documento. E aqui está o recurso que fecha o ciclo de qualidade: "Ver Diferenças" — as duas versões lado a lado, em verde o que entrou, em vermelho o que saiu. Dá pra ver, linha a linha, exatamente o que mudou no croqui do caso de uso seis — e mais nada. Rastreável até na correção.
**PRODUÇÃO:** mostrar a instrução digitada no chat (28), o estado "Gerando Refinamento" (29), e então o **DiffViewerModal** lado a lado (30) rolado até a mudança do wireframe do UC-006 — verde "Detalhe do Caso"/"Calcular Escore de Risco"/"APACHE II", vermelho "Painel de Prognóstico"/"Recalcular".
**TELA:** 28_refine_instrucao.png, 29_refine_processando.png, 30_diff_uc006.png, 30b_diff_uc006_botao.png
**NOTA PRODUÇÃO:** o refino leva minutos (qwen local) — cortar para o diff. 30b mostra as TRÊS mudanças num quadro só: linha 505 `Painel de Prognóstico`→`Detalhe do Caso` (vermelho→verde), linha 517 `+ APACHE II: 18` (adicionada, só verde), linha 519→520 `[ Recalcular ]`→`[ Calcular Escore de Risco ]` — e todo o resto idêntico dos dois lados (correção cirúrgica). O agente aplicou exatamente as 3 instruções, gerou a **Versão 2**; correção veio do próprio gerador (não editada à mão).

## Cena 11 — Etapa 3: Modelo de Dados (deriva da Especificação) + validação que corrige
**NARRAÇÃO:** Da Especificação nasce o Modelo de Dados. Repare no topo: "Origem: Especificação zero-oito-cê" — de novo a amarração explícita à etapa anterior. A ferramenta leu a seção seis da spec e extraiu, sozinha, oito entidades: paciente, caso, microbiologia, escore de risco, tratamento, alerta, usuário e log de auditoria. E gerou três artefatos ao mesmo tempo: o schema SQL, os modelos em SQLAlchemy e Pydantic, e a migração Alembic. Veja a rastreabilidade viva: a tabela paciente tem a coluna "apache dois", exatamente o parâmetro que o fluxo de Cox exigia lá na especificação. Mas o mais interessante é o painel de validação: a ferramenta não só gera, ela CONFERE — e apontou um problema grave, nota vinte e cinco de cem: "o schema não aplica no MySQL", porque os campos booleanos vieram com valor padrão inválido. Pedimos a correção ao agente, e ele reescreveu só os defaults. A nota saltou para noventa e dois, o schema passou a aplicar, e sobraram apenas três observações menores. Validou, achou, corrigiu.
**PRODUÇÃO:** mostrar (antes) shot 41 com "Validação (score 25/100) — 4 problemas" e o [high] "O SCHEMA NÃO APLICA no MySQL"; destacar em Entidades a coluna **apache_ii** em paciente (rastreabilidade ao UC-006). Depois "Refinar com o agente" com a instrução dos booleanos → (depois) shot 44 "score 92/100 — 3 problemas" e `classificacao_nhsn BOOLEAN NN 0`. Abas Entidades / Schema SQL / models.py / Alembic / YAML.
**TELA:** 40_dm_landing.png, 41_dm_entidades.png (antes, 25/100), 42_dm_schema.png, 43_dm_models.png, 44_dm_validado_92.png (depois), 45_dm_schema_fixed.png
**NOTA PRODUÇÃO:** ótimo 2º exemplo do laço validar→refinar (agora no schema, não na tela). Origem: Especificação 08c001bb; 8 tabelas; DBMS MySQL (banco local).

## Cena 12 — Etapa 4: Interface & Protótipo (a rastreabilidade que se paga)
**NARRAÇÃO:** A quarta etapa transforma cada caso de uso numa tela de verdade. De novo "Origem: Especificação zero-oito-cê": a ferramenta leu os doze casos de uso e o modelo de dados, e gerou doze telas — cada uma com o seu tipo (formulário, detalhe, tabela, dashboard) e amarrada à sua tabela no banco. E aqui está a recompensa da rastreabilidade que corrigimos lá atrás: abra a tela do caso de uso seis. Ela se chama "Detalhe do Caso" — o nome certo. O botão é "Calcular Escore de Risco" — o texto certo. E nos fatores de risco aparece o "APACHE dois". Exatamente a correção que pedimos no croqui da especificação, propagada sozinha até o protótipo renderizado. Foi uma mudança numa etapa, que desceu coerente por todas as seguintes. Isso é desenvolvimento orientado a especificação.
**PRODUÇÃO:** mostrar a landing (12 telas listadas com UC + tipo + tabela) — rótulo "12 CASOS DE USO → 12 TELAS, cada uma ligada a uma tabela". Depois clicar em **UC-006 Detalhe do Caso** e destacar no mockup renderizado: título "Detalhe do Caso", botão "Calcular Escore de Risco", "APACHE II: 18" (verde/seta ligando ao diff da Cena 10). Depois UC-009 Dashboard (tabela CRUD sobre `casos`) e UC-005 Alerta MDR. Botão "Verificar coerência".
**TELA:** 50_uispec_landing.png, 53_uispec_uc006_cox.png, 54_uispec_uc009_dash.png, 55_uispec_uc005_mdr.png
**NOTA PRODUÇÃO:** ESTE é o momento-chave da palestra: a correção do croqui (Cena 10) reaparece fiel no protótipo. Ligar visualmente 30b (diff) → 53 (mockup). Geração de 12 telas leva ~12min (qwen local) — cortar para o resultado.

## Cena 13 — Etapa 5: Agentes & Tarefas (quem faz o quê, e com quais ferramentas)
**NARRAÇÃO:** Chegamos ao coração agêntico. A partir da Especificação — de novo "Base: Especificação versão dois" —, a ferramenta projeta a equipe de agentes e as tarefas. Ela gerou um documento com onze agentes e treze tarefas. Abra e veja o nível de detalhe de cada agente: papel, objetivo, história de fundo, o modelo de linguagem, as **ferramentas**, a delegação, e — importante — a **justificativa rastreável**: para qual requisito aquele agente existe. Veja o agente de prognóstico, o Calculador de Cox: o objetivo é obter o escore de risco; a história diz explicitamente que ele "chama a API externa escore de risco de Cox"; e a justificativa aponta para os requisitos funcionais um e oito. Repare que dois agentes precisam de serviços que vivem FORA do sistema: o de microbiologia, que consulta o laboratório, e o de Cox, que chama o motor estatístico. É aqui que entram as ferramentas MCP.
**PRODUÇÃO:** mostrar o painel (Base: Especificação v2, Frameworks: CrewAI, "Gerar Agentes & Tarefas") e o chat "✅ Documento gerado: 11 agentes e 13 tarefas". Abrir 👁️ Visualizar e destacar o agente de **Cox** (Goal, Backstory citando `escore_risco_cox`, Tools, Rationale "FR-001 e FR-008") e o agente de **microbiologia** citando `consultar_microbiologia`. Rótulo "CADA AGENTE RASTREÁVEL A UM REQUISITO".
**TELA:** 60_ats_doc_landing.png, 63_atsdoc_topo.png, 65_atsdoc_cox.png, 66_atsdoc_micro.png, 67_atsdoc_tarefas.png
**NOTA PRODUÇÃO:** gancho para a Cena 14 (MCP): os dois agentes que chamam serviços externos = as duas tools MCP. Documento `especificacao_agentes_tarefas.md` (34KB), sessão bd39f85e.

## Cena 14 — As ferramentas MCP (integrações externas reais)
**NARRAÇÃO:** Os agentes de microbiologia e de Cox precisam falar com sistemas de fora — o laboratório e o motor estatístico. É exatamente para isso que serve o M C P, o Model Context Protocol. Na Configuração Global de M C P, registramos um servidor: o "BioByte Sentinela — Integrações", que expõe duas ferramentas. Ao clicar em Testar, a ferramenta faz o handshake real com o servidor e DESCOBRE, sozinha, as duas tools: "consultar microbiologia" e "escore de risco de Cox". O servidor fica ativo. Depois, no projeto, ligamos cada tool ao agente certo: a consulta de microbiologia ao agente de ingestão de dados, e o escore de Cox ao agente de prognóstico. Guarde este ponto: quando chegarmos à geração de código, essas duas atribuições viram um arquivo de ferramentas M C P de verdade, que chama o servidor externo.
**PRODUÇÃO:** MCP → Configuração Global. Mostrar o texto de topo ("Testar conexão faz o handshake e descobre as ferramentas"). Rolar até "Servidores registrados" e destacar o card **BioByte Sentinela - Integrações (LIS + Cox) · ativo · sse · 2 tool(s)** com os chips **consultar_microbiologia** e **escore_risco_cox**. Mostrar o banner "Ativo — 2 ferramenta(s) descoberta(s)". Depois a atribuição por agente (projeto → MCP).
**TELA:** 70_mcp_global.png, 71b_mcp_biobyte_tools.png, 73_mcp_biobyte_2tools.png, 72_mcp_registrados.png
**NOTA PRODUÇÃO:** servidor real em `docs/apresentacao/demo-biobyte/mcp/biobyte_mcp_server.py` (FastMCP SSE :9120). Atribuições: cox_risk_calculation_agent→escore_risco_cox; microbiology_ingestion_agent→consultar_microbiologia. GANCHO: Cena de Geração de Código mostra o `mcp_tools.py` emitido a partir daqui.

## Cena 15 — Etapa 6: YAML de Agentes e Tarefas (o que o CrewAI executa)
**NARRAÇÃO:** Do documento de agentes e tarefas, a ferramenta gera os dois arquivos que o CrewAI realmente executa: o agents.yaml e o tasks.yaml. No agents.yaml, cada agente vira uma definição concreta — papel, objetivo, história, se pode delegar. Veja o integrador de microbiologia, que busca as culturas no laboratório externo. E no tasks.yaml está o ouro: cada tarefa traz, no topo, a sua rastreabilidade — a qual caso de uso e a qual requisito ela atende. Veja a tarefa de autenticação: caso de uso um, requisito funcional dez. Traz também se a execução é determinística ou agêntica, qual agente a executa, os passos, e a saída esperada. É um artefato pronto para virar código.
**PRODUÇÃO:** página "YAML de Agentes e Tarefas", card agents.yaml (10KB) → 👁️ Visualizar mostrando `auth_manager_agent` e `microbiology_integrator_agent` (role/goal/backstory). Aba Tasks YAML → Visualizar mostrando `authenticate_user_mfa` com **traceability: uc: UC-001, fr: FR-010**, `execution: deterministic`, `agent: auth_manager_agent`. Rótulo "RASTREABILIDADE ATÉ NA TAREFA (uc + fr)".
**TELA:** 80_yaml_card.png, 80b_yaml_agents_content.png, 81_yaml_tasks.png
**NOTA PRODUÇÃO:** agents.yaml 11 agentes; tasks.yaml 13 tarefas (inclui `fetch_and_persist_microbiology`, `calculate_cox_risk_score` que usam as tools MCP, e `orquestrar_fluxo_clinico_integrado`). Campo `execution` = roteamento híbrido determinístico/agente.

## Cena 16 — Etapa 7: Sequência de Tarefas (como o fluxo roda)
**NARRAÇÃO:** Antes de virar código, a ferramenta desenha a sequência de execução: como as treze tarefas e os onze agentes se encadeiam. Ela identificou um fluxo misto — um pipeline principal linear (autenticar, importar, classificar, calcular risco, recomendar) mais fluxos paralelos de administração e monitoramento — e define o estado compartilhado que passa de tarefa em tarefa, no estilo LangGraph. É o roteiro de execução que o motor agêntico vai seguir.
**PRODUÇÃO:** página "Sequência de Tarefas", card task_flow (39KB) → 👁️ Visualizar. Destacar "1. Visão Geral" (Total de Tasks 13, Total de Agentes 11, Tipo de Fluxo Misto, Modelo de State Acumulativo LangGraph-style) e a tabela do State (email/senha/codigo_mfa...).
**TELA:** 82_flow_landing.png, 83_flow_doc.png
**NOTA PRODUÇÃO:** documento task_flow_20260901_024105 (39KB), 39802 chars. Deriva de tasks.yaml (8fa9cbcc) + spec + ATS doc.

## Cena 17 — Etapa 8: Rede de Petri (o modelo formal executável)
**NARRAÇÃO:** A ferramenta então converte esse fluxo numa Rede de Petri — o modelo matemático clássico para processos concorrentes. Repare no cabeçalho: "gerada a partir de agents.yaml mais tasks.yaml mais a Sequência de Tarefas" — mais uma amarração. Cada lugar é um estado, cada transição é uma tarefa que dispara, e a ficha, o token, marca onde o fluxo está. Aqui: quinze lugares, dezessete transições, trinta e cinco arcos. Começa no "Início do Fluxo", passa por Autenticar, Importar Microbiologia, Classificar, Calcular Risco… e é isso que a ferramenta usa para orquestrar e, inclusive, simular a execução — tem até o botão "Simular" e "Execução Real".
**PRODUÇÃO:** página "Rede de Petri" com o editor visual (JointJS). Mostrar o token no "Início do Fluxo", os lugares (círculos) e transições (barras) rotulados em português (Autenticar Usuário MFA, Buscar e Persistir Microbiologia, etc.), e a barra de ferramentas (Gerar Rede, Simular, ▶ Execução Real, Visualizar JSON). Rótulo "15 lugares · 17 transições · 35 arcos".
**TELA:** 84_petri_landing.png, 85_petri_net.png
**NOTA PRODUÇÃO:** Petri derivada de agents.yaml (01b04782) + tasks.yaml (8fa9cbcc) + Sequência (38e454fb). É editável e simulável na própria tela.


## Cena 17b — Validação da Petri: bipartição (achado + correção no gerador)
**NARRAÇÃO:** E vale mostrar mais uma validação. Rede de Petri é um grafo BIPARTIDO: arco só liga lugar a transição, nunca transição a transição. Na primeira geração, o modelo cometeu esse erro nos nós de término. A ferramenta agora tem uma guarda determinística que detecta e corrige: reclassifica o nó de término errado como lugar e reamarra o fluxo, garantindo a topologia. Regeramos, e a rede fica correta — lugar e transição sempre alternando.
**PRODUÇÃO:** contrastar (antes) arco transição→transição nos nós de "fim" com (depois) a rede corrigida, lugar→transição→lugar. Rótulo "PETRI É BIPARTIDO: só lugar↔transição".
**TELA:** 85_petri_net.png (rede corrigida)
**NOTA PRODUÇÃO:** fix no gerador `_enforce_bipartite` + checagem `bipartite_violation` no validador. Rede final: 15 lugares, 17 transições, 34 arcos, 0 violações.

## Cena 18 — Etapa 9: Geração de Código (o sistema nasce da cadeia)
**NARRAÇÃO:** E aqui o código nasce da cadeia inteira. A partir do agents.yaml, do tasks.yaml e da Rede de Petri, a ferramenta gerou oitenta e cinco arquivos: o servidor WebSocket que orquestra os agentes, o adapters.py com as funções de entrada e saída de cada tarefa, o tools.py, o schema do banco, as telas React, o motor de Petri no frontend, Docker, tudo. E lembra das duas ferramentas MCP que ligamos? Elas viraram código real: o arquivo mcp_tools.py, auto-gerado, com uma classe BaseTool do CrewAI para cada tool — consultar microbiologia e escore de risco de Cox — que chama o servidor MCP externo via o cliente do protocolo. As atribuições que fizemos na tela viraram código executável. E tem o botão Executar, que sobe o sistema.
**PRODUÇÃO:** página "Geração de Código" — cabeçalho, "Sessão: 85 arquivos completed", a árvore de ARQUIVOS (85) com destaque em ws-server/main.py, adapters.py, e **ws-server/mcp_tools.py**. Abrir o mcp_tools.py e mostrar `class _MCPArgs`, `_mcp_call(...)` via `sse_client`, `class MCPTool_0 name="consultar_microbiologia"`, `MCPTool_1 name="escore_risco_cox"`, `MCP_TOOLS = {...}`. Rótulo "AS 2 TOOLS MCP VIRARAM CÓDIGO". Botão ▶ Executar.
**TELA:** 86_codegen_landing.png, 87_codegen_mcp_tools.png, 87b_codegen_mcp_tools_bottom.png, 88_codegen_adapters.png
**NOTA PRODUÇÃO:** 85 arquivos, sessão code_gen_20260901_064051. mcp_tools.py chama http://127.0.0.1:9120/sse (o servidor MCP real). Ver arquivo entregue em mcp/mcp_tools_GERADO_pelo_codegen.py.

## Cena 19 — Etapa 10: Casos de Teste & Validação (Grafo de Causa-Efeito)
**NARRAÇÃO:** Documento gerado, sistema gerado — falta validar. E aqui a ferramenta aplica uma técnica clássica de teste: o Grafo de Causa-Efeito. De novo "Origem: Especificação zero-oito-cê": para cada caso de uso, ela extrai as causas — as ações do ator — e os efeitos — as respostas do sistema —, monta o grafo lógico e deriva a tabela de decisão. Cada coluna da tabela vira um caso de teste. Foram onze casos de uso cobertos e quarenta e três casos de teste. Veja o caso de uso um, autenticação: as causas são credenciais válidas, código correto, código expirado, pedido de reenvio; os efeitos, o sistema respondendo a cada combinação. Cada caso de teste traz suas entradas e a saída esperada. Repare que o caso de uso dez está marcado como "falhou" — a ferramenta é honesta sobre onde a geração não fechou, em vez de inventar.
**PRODUÇÃO:** página "Casos de Teste & Validação". Cabeçalho explicando a técnica (causas = ações do ator, efeitos = respostas). "11 casos de uso · 43 casos de teste". Lista de UCs com contagem (UC-006 Cox = 3 casos). Selecionar UC-001 e mostrar o **Grafo de Causa-Efeito** (CAUSAS c1..c4 ligadas aos efeitos, com negações). Botão 📄 Documento (documento de validação, 90KB) e ✓ Aprovar. Destacar UC-010 = "falhou" (transparência).
**TELA:** 90_testcases_landing.png, 91_testcases_uc006.png, 92_testcases_validacao.png
**NOTA PRODUÇÃO:** sessão c99867c4; 11/12 UCs, 43 casos, doc de validação 90.491 chars. UC-010 CEG falhou (1 UC) — mostrado como falhou. Técnica CEG = causas→efeitos→tabela de decisão→casos.

## Cena 20 — E2E: o sistema rodando (app + MCP)
**NARRAÇÃO:** E o sistema roda. Extraímos os oitenta e cinco arquivos, criamos o banco, e subimos o servidor do aplicativo. Repare na inicialização: "duas ferramentas MCP carregadas". E elas funcionam de verdade: quando o agente de prognóstico executa, ele chama o serviço externo de Cox pelo MCP — o log mostra as chamadas ao servidor na porta nove mil cento e vinte — e recebe o escore de risco calculado; e a consulta de microbiologia devolve o Staphylococcus aureus multirresistente, com o antibiograma. O aplicativo gerado, do requisito ao código, conversa com os sistemas externos via o protocolo que configuramos na tela.
**PRODUÇÃO:** terminal com o boot do ws-server ("[ws] 2 tool(s) MCP carregada(s)", "BioByte Sentinela em ws://localhost:5030"); a chamada `escore_risco_cox(...)` retornando `{escore_cox, nivel_risco, modelo: "Cox proportional hazards"}`; `consultar_microbiologia("CAS-2023-001")` retornando o patógeno + perfil; e o log do agente chamando `escore_risco_cox` (:9120) durante a task.
**TELA:** (capturas de terminal — boot do app, retorno das tools MCP, log do agente)
**NOTA PRODUÇÃO — honestidade técnica (estado em 01/09; SUPERADO pelas Cenas 21–24):** app roda (ws-server :5030, banco biobyte_app 8 tabelas, 2 tools MCP), tools MCP provadas E2E, agente chama a MCP. ARESTA: task combinada (SQL + tool) não persiste limpo — qwen gera SQL inválido no passo de banco (fragilidade de SQL agêntico com modelo local; padrão do gerador é determinístico-first). 2 gaps de gerador achados: (1) task com tool externa marcada `deterministic`; (2) code-gen não liga a tool MCP no AGENT_TOOLS do agente.


## Cena 21 — O que o teste ensinou: coerência entre as peças (correções no gerador)
**NARRAÇÃO:** Aquele primeiro teste completo foi valioso justamente pelo que falhou. Só três das treze tarefas passavam. E a causa, quando fomos ao fundo, era uma só: as peças que o gerador produz — a lista de tarefas, o código que grava no banco, o modelo de dados e as ferramentas externas — não falavam exatamente a mesma língua. A ferramenta externa devolvia "perfil de resistência"; a coluna do banco se chamava "sensibilidades". O código lia uma chave com um nome; o valor circulava com outro. E tarefas que precisavam calcular um resultado — classificar o caso pela norma, recomendar o protocolo — tinham sido marcadas como procedimento fixo, que não calcula nada, só copia. Corrigimos isso no gerador, não no aplicativo: um passo de coerência que alinha os nomes entre todas as peças; uma busca automática no sistema externo antes de gravar, quando a tarefa depende dele; o repasse do contexto do caso ao longo da cadeia; e um portão que detecta a tarefa marcada como fixa que na verdade precisa pensar — e a reclassifica, registrando o motivo. E o modelo de linguagem também mudou: saímos do modelo local, que derrubou a placa de vídeo, para o DeepSeek em nuvem, sem modo de raciocínio, com a troca a um passo de distância.
**PRODUÇÃO:** quadro "ANTES 3/13 → DEPOIS 12/13" e a lista das quatro correções de gerador (coerência de nomes · busca externa antes de gravar · contexto ao longo da cadeia · portão de classificação). Rótulo "CORRIGIDO NO GERADOR, NÃO NO APP". Opcional: trecho do commit log mostrando os commits da série "Coerência execução (1..5)".
**TELA:** (cartela) — pode usar o terminal do placar E2E 3/13 vs 12/13 (docs/apresentacao/demo-biobyte/e2e/)
**NOTA PRODUÇÃO:** commits 826e895 (DeepSeek v4-flash sem raciocínio), b8fbded (tool MCP no AGENT_TOOLS), c999f97 (prefetch MCP + carry-forward), edf6501, 2bc3b71/b01fbdc/69799dd/e2f0b6b (coerência intra-SQL), 416421d/251de41 (aliases MCP via LLM), 1a91798 (portão de classificação), 0956727 (tela↔tarefa por caso de uso), cad6d1a (porta do frontend). Placar validado com o roteiro e2e/petri_flow.py.

## Cena 22 — Regeneração limpa: o gerador prova que aprendeu (LangNet)
**NARRAÇÃO:** A prova de que as correções estão no gerador — e não em remendo manual — é regenerar do zero. Mesmos insumos aprovados: os mesmos agentes, a mesma lista de tarefas, a mesma Rede de Petri. Veja a página de Geração de Código: sete sessões, as duas de hoje ao lado das de ontem, cada uma com histórico de versões. E dentro do código gerado dá para ver o que a ferramenta fez sozinha. No arquivo das ferramentas externas, a tabela de tradução de nomes, preenchida automaticamente: "perfil de resistência" para "sensibilidades", "escore de Cox" para "valor do escore". Na lista de tarefas, a classificação NHSN aparece como tarefa de agente — com o motivo escrito pelo portão: "o código fixo só copia este valor da entrada; pela descrição, esta tarefa deve determiná-lo". Baixamos os oitenta e quatro arquivos, subimos o sistema sem tocar em nada, e rodamos a cadeia inteira: doze de treze. A décima terceira é a administração de usuários, que só precisa dos dados do usuário a cadastrar. O sistema clínico completo roda.
**PRODUÇÃO:** página Geração de Código com as 7 sessões (destacar code_gen_20260902_172156 = v7 e _160212 = v6); modal "Histórico de versões"; arquivo mcp_tools.py rolado até MCP_ARG_ALIASES/MCP_OUT_ALIASES; arquivo tasks.yaml em classify_case_nhsn mostrando `execution: agent` e `execution_reason`; botão ▶ Executar em evidência. Fechar com o terminal do E2E "12/13 tarefas OK".
**TELA:** 200-langnet-codegen-pagina.png, 201-langnet-versesoeshistorico.png, 206-langnet-mcp_tools-aliases.png, 207-langnet-tasks_yaml-portao-nhsn.png, 202/203/204/205 (arquivos), 208-langnet-adapters-alinhamento.png
**NOTA PRODUÇÃO:** sessão v7 = 9bfaed71-eebf-4eb6-97f0-f8de402e7911 (85 arq.). Deploy limpo via ZIP do mesmo endpoint do botão da interface (e2e/deploy_v6.sh). Verificação zero-mão: classify=agent [portão], aliases preenchidos, alinhamentos presentes, prefetch + carry-forward no servidor. E2E 12/13 (P10 = admin, passa com nome/email).

## Cena 23 — O sistema por dentro: quatro módulos, vinte telas
**NARRAÇÃO:** Este é o sistema que nasceu da especificação. Vinte telas em quatro módulos. Atendimento, com o login, o alerta de multirresistência, o detalhe do caso e a auditoria. Engajamento, com a visão de classificação do caso. Relatórios. E Cadastros, com as telas de negócio e o cadastro de cada entidade do modelo de dados. Nas próximas cenas eu abro uma por uma: o que a tela é, o que o operador faz nela, e o que o sistema responde. Cada tela mostra, embaixo do título, qual caso de uso implementa e qual agente ela aciona — a rastreabilidade que começou no requisito chega até o botão.
**PRODUÇÃO:** percorrer a barra lateral devagar pelos quatro grupos, sem entrar em nenhuma tela ainda. Rótulo: "20 TELAS · 4 MÓDULOS · CADA UMA LIGADA A UM CASO DE USO".
**TELA:** 694-app-home.png

## Cena 23.1 — Login com verificação em duas etapas (UC-001)
**NARRAÇÃO:** A porta de entrada. E-mail, senha e o código de verificação em duas etapas. Entro primeiro com uma senha errada, de propósito: o sistema responde "Credenciais inválidas" — a mesma frase que o caso de uso manda exibir. Isso não é detalhe de estilo. Antes desta rodada o sistema devolvia o texto interno da verificação, aquele jargão que ninguém entende, direto na cara do usuário. Agora quem opera lê a mensagem do negócio, e o detalhe técnico fica guardado para quem sustenta o sistema. Com as credenciais certas, o agente de autenticação responde, registra a entrada na auditoria, e o painel "atendimento corrente" aparece: daqui em diante o sistema sabe quem entrou e qual caso está aberto, e leva esse contexto de tela em tela.
**PRODUÇÃO:** mostrar a tentativa com senha errada e a mensagem "Credenciais inválidas" em destaque; depois a entrada correta e o painel de contexto se preenchendo. Rótulo: "A MENSAGEM É A DO CASO DE USO, NÃO O ERRO INTERNO".
**TELA:** 650-validacao-login-credenciais-invalidas-antes.png, 651-validacao-login-credenciais-invalidas.png, 868-app-form-login-mfa-preenchido.png, 869-app-form-login-mfa-resposta.png

## Cena 23.2 — Importação de microbiologia (UC-002)
**NARRAÇÃO:** A enfermeira de controle de infecção abre a importação. Repare no alto: paciente e caso já estão preenchidos, herdados do contexto — ela não redigita nada disso. O único campo que resta é o identificador da amostra. Ela clica em "Consultar Externo" e o sistema abre a sessão de importação. É uma tela curta de propósito: o trabalho pesado é do agente, não do formulário.
**PRODUÇÃO:** destacar os dois campos de leitura no topo (paciente e caso) vindos do contexto, o campo da amostra e o botão. Rótulo: "O CONTEXTO CLÍNICO VIAJA COM O USUÁRIO".
**TELA:** 870-app-form-importacao-microbiologia-preenchido.png, 871-app-form-importacao-microbiologia-resposta.png

## Cena 23.3 — Prévia dos resultados do laboratório (UC-003)
**NARRAÇÃO:** Aqui o sistema conversa com o laboratório de verdade, por um servidor externo no protocolo MCP. Volta o microrganismo — Staphylococcus aureus — e o antibiograma, com o perfil de resistência. A tela mostra isso em campos de leitura, com nome de negócio, não como um bloco de dados cru. E se o laboratório devolver um resultado fora do padrão da norma, o sistema bloqueia a importação e diz exatamente isso: dados não conformes recebidos do sistema externo, com a lista do que faltou. Não grava pela metade.
**PRODUÇÃO:** mostrar a resposta com microrganismo e sensibilidades; em seguida a tentativa com dado não conforme e a mensagem de bloqueio. Rótulo: "LABORATÓRIO EXTERNO VIA MCP · DADO FORA DO PADRÃO NÃO ENTRA".
**TELA:** 872-app-form-previa-resultados-microbiologia-preenchido.png, 873-app-form-previa-resultados-microbiologia-resposta.png

## Cena 23.4 — Classificação do caso pela norma NHSN (UC-004)
**NARRAÇÃO:** Esta é a tela que decide se o caso é uma infecção de corrente sanguínea associada a cateter. O caso de uso pede três coisas na tela: os dados do paciente, o selo do resultado, e as marcações dos três critérios da norma — cateter presente, hemocultura positiva, correlação clínica. Está tudo aqui, e as marcações não são enfeite: elas refletem o que o agente respondeu. Ele aplica a regra, devolve a classificação e escreve a justificativa citando os critérios que encontrou e os que faltaram. Neste caso: cateter presente sim, hemocultura positiva não — logo, não é ICSAC, e o sistema diz por quê.
**PRODUÇÃO:** parar na justificativa do agente e ler em voz alta a regra citada. Apontar as três marcações e o selo. Rótulo: "A NORMA APLICADA, COM A JUSTIFICATIVA ESCRITA".
**TELA:** 874-app-form-detalhe-caso-clinico-classificacao-preenchido.png, 875-app-form-detalhe-caso-clinico-classificacao-resposta.png

## Cena 23.5 — Alerta de multirresistência (UC-005)
**NARRAÇÃO:** Quando o microrganismo é multirresistente, o sistema abre um alerta. A tela traz o tipo de alerta, a descrição, a data, o microrganismo e as classes de resistência. O registro do alerta é gravado no banco e entra na auditoria. Aqui eu preciso ser honesto: o caso de uso também pede envio por e-mail e notificação por push, e isso **não está implementado** — o sistema registra o alerta, mas não avisa ninguém por fora da tela. Está no relatório de validação, com essas palavras.
**PRODUÇÃO:** mostrar o alerta criado e, na cartela, a ressalva "envio por e-mail e push: não implementado". Rótulo: "O QUE FALTA TAMBÉM É DITO".
**TELA:** 876-app-form-alerta-mdr-preenchido.png, 877-app-form-alerta-mdr-resposta.png

## Cena 23.6 — Escore de risco pelo modelo de Cox (UC-006)
**NARRAÇÃO:** O detalhe do caso calcula o escore de risco pelo modelo de riscos proporcionais de Cox, num serviço externo. A tela mostra o escore como barra de progresso — vinte e quatro por cento, risco baixo — junto com os fatores que pesaram: idade acima de sessenta e cinco, internação em unidade de terapia intensiva, nutrição parenteral, cateter central há doze dias. E se faltar qualquer um dos parâmetros clínicos, o sistema **recusa e lista o que falta**, em vez de calcular com o que tem. Num sistema clínico, isso é obrigação, não delicadeza.
**PRODUÇÃO:** mostrar a barra do escore e a lista de fatores; depois a recusa com os campos faltantes. Rótulo: "SEM O DADO CLÍNICO, O SISTEMA RECUSA".
**TELA:** 878-app-form-detalhe-caso-preenchido.png, 879-app-form-detalhe-caso-resposta.png, 675-validacao-escore-barra-progresso-antes.png, 676-validacao-escore-barra-progresso.png

## Cena 23.7 — Recomendação do protocolo de tratamento (UC-007)
**NARRAÇÃO:** Esta é a tela onde o sistema mais se parece com um colega experiente. O agente recomenda o protocolo — "Bundle MRSA Vancomicina" — e escreve a justificativa clínica inteira: paciente de setenta e dois anos, em terapia intensiva, cateter central há doze dias, nutrição parenteral, APACHE dezoito, hemocultura positiva para Staphylococcus aureus multirresistente, escore de risco calculado. E lista o que o protocolo inclui: antibioticoterapia dirigida, remoção do cateter suspeito, precaução de contato, monitoramento seriado. Antes desta rodada, esta tela tinha um título e um botão. Só isso. O texto que você está lendo agora vinha do agente e era jogado fora pela interface.
**PRODUÇÃO:** ler a justificativa clínica na tela, devagar. Mostrar lado a lado o "antes" (título e botão) e o "agora". Rótulo: "ANTES: TÍTULO E BOTÃO · AGORA: A RECOMENDAÇÃO INTEIRA".
**TELA:** 628-app-tela-recomendacao-bundle.png, 880-app-form-recomendacao-bundle-preenchido.png, 881-app-form-recomendacao-bundle-resposta.png

## Cena 23.8 — Estimativa de redução de risco (UC-008)
**NARRAÇÃO:** Aprovado o protocolo, o sistema estima o quanto ele reduz o risco: trinta e cinco por cento, com intervalo de confiança de noventa e cinco por cento entre vinte e cinquenta por cento. É exatamente o que o caso de uso desenhou. E quando não existe tratamento aprovado para aquele caso, o agente **declara que os dados são insuficientes** e diz o que falta — em vez de inventar um número. Essa foi uma das correções mais importantes desta rodada: o sistema pedia ao agente, na segunda tentativa, que preenchesse os campos obrigatórios de qualquer jeito, e isso empurrava para a invenção.
**PRODUÇÃO:** mostrar o valor e o intervalo; depois o caso sem tratamento, com a recusa honesta. Rótulo: "RECUSA HONESTA EM VEZ DE NÚMERO INVENTADO".
**TELA:** 882-app-form-resultado-estimativa-risco-preenchido.png, 883-app-form-resultado-estimativa-risco-resposta.png

## Cena 23.9 — Painel de vigilância (UC-009)
**NARRAÇÃO:** O painel que a comissão de controle de infecção olha todo dia. Três indicadores: casos confirmados nos últimos trinta dias, taxa de multirresistência e escore médio de risco. Abaixo, a evolução dos casos e a distribuição por microrganismo. Os números vêm do banco do sistema, não de exemplo. E repare no que os gráficos dizem quando ainda não há série no período: "sem dados para o período". Eles não desenham uma linha bonita do nada. Esta tela, até ontem, era um título e um botão — e ela é o coração da vigilância.
**PRODUÇÃO:** abrir com os três cartões preenchidos, descer para os dois gráficos. Mostrar em contraste a versão anterior, vazia. Rótulo: "TRÊS INDICADORES E DOIS GRÁFICOS, COMO A ESPECIFICAÇÃO PEDE".
**TELA:** 630-app-tela-dashboard-vigilancia.png, 884-app-form-dashboard-vigilancia-preenchido.png, 885-app-form-dashboard-vigilancia-resposta.png

## Cena 23.10 — Gestão de usuários e permissões (UC-010)
**NARRAÇÃO:** O administrador cadastra quem usa o sistema: nome, e-mail, papel e senha — agora em campo de senha, oculto, como tem que ser. E, ao lado do formulário, a tabela de usuários que a especificação pede: quem existe, com que e-mail e em que papel. Antes só havia o formulário; não dava para ver quem já estava cadastrado. A senha nunca trafega em texto: o próprio sistema deriva o resumo criptográfico antes de gravar.
**PRODUÇÃO:** mostrar o cadastro sendo feito e a tabela de usuários. Rótulo: "CADASTRO E LISTA, COMO NO DESENHO APROVADO".
**TELA:** 631-app-tela-gestao-de-usuarios.png, 886-app-form-gestao-de-usuarios-preenchido.png, 887-app-form-gestao-de-usuarios-resposta.png

## Cena 23.11 — Auditoria (UC-011)
**NARRAÇÃO:** Tudo o que acontece no sistema fica registrado: quem entrou, quem classificou, quem aprovou um protocolo, de qual endereço e quando. A tela filtra por período, usuário e tipo de ação, mostra a tabela e exporta em CSV. E quando o filtro não encontra nada, ela diz "nenhum registro encontrado para os filtros aplicados" — que parece óbvio, mas era um defeito real: a consulta devolvia vazio mesmo havendo registros, porque o sistema guardava uma única célula da primeira linha em vez do conjunto de resultados.
**PRODUÇÃO:** filtrar um período sem registros e mostrar a mensagem; depois um período com registros e a tabela preenchida, e o botão de exportação. Rótulo: "VAZIO É VAZIO, NÃO É ERRO".
**TELA:** 677-validacao-auditoria-nenhum-registro-antes.png, 678-validacao-auditoria-nenhum-registro.png, 888-app-form-logs-auditoria-preenchido.png, 889-app-form-logs-auditoria-resposta.png

## Cena 23.12 — Geração de relatórios (UC-012)
**NARRAÇÃO:** Por fim, o relatório de vigilância. Escolho o formato, o período e o filtro de paciente, e o sistema gera o arquivo — em PDF ou em CSV — e devolve o caminho. Também aqui havia um defeito de fundo: a tarefa estava marcada como procedimento fixo, e procedimento fixo não chamava ferramenta nenhuma. O sistema devolvia os dados da consulta e arquivo nenhum. Hoje o arquivo existe, e é o mesmo caminho que aparece na resposta.
**PRODUÇÃO:** gerar em CSV e mostrar o caminho do arquivo na resposta; abrir o arquivo gerado se possível. Rótulo: "O RELATÓRIO VIRA ARQUIVO DE VERDADE".
**TELA:** 890-app-form-geracao-relatorios-preenchido.png, 891-app-form-geracao-relatorios-resposta.png

## Cena 23.13 — Os cadastros de cada entidade do modelo de dados
**NARRAÇÃO:** Além das telas de negócio, o sistema traz um cadastro completo para cada entidade que saiu do modelo de dados: pacientes, casos, microbiologias, alertas, escores de risco, tratamentos, usuários e o registro de auditoria. Cada um com lista, busca, ficha, criação, edição e exclusão com confirmação. Não é enfeite: é por aqui que se corrige um dado errado sem abrir o banco.
**PRODUÇÃO:** ritmo rápido pelos oito cadastros, parando em Pacientes para mostrar lista, ficha e formulário. Rótulo: "8 CADASTROS COMPLETOS, VINDOS DO MODELO DE DADOS".
**TELA:** 707-app-tela-alertas.png, 708-app-tela-casos.png, 709-app-tela-escores_risco.png, 710-app-tela-logs_auditoria.png, 711-app-tela-microbiologias.png, 712-app-tela-pacientes.png, 713-app-tela-tratamentos.png, 714-app-tela-usuarios.png

## Cena 24 — Por que as telas estavam pobres, e onde isso foi corrigido
**NARRAÇÃO:** Vale contar o que aconteceu entre a primeira versão destas telas e a que você acabou de ver, porque é o ponto mais instrutivo do projeto. A especificação estava certa. A especificação de interface, gerada a partir dela, também: declarava para o painel três cartões de indicador e dois gráficos, para a recomendação a justificativa e a lista de itens, para a gestão de usuários a tabela, para a classificação as marcações da norma. O erro estava no gerador de código: ele só sabia desenhar campo de texto, número, data e seleção, e **descartava calado** todo o resto. Sete das doze telas de negócio nasciam com um título e um botão. Havia até um resto de outro projeto, pedindo para arrastar um arquivo geoespacial numa tela de hospital. A correção foi feita no gerador — e a prova é esta: mesma especificação, mesmos agentes, nada tocado à mão no aplicativo, e as telas passam a nascer com o que o desenho aprovado sempre disse.
**PRODUÇÃO:** tela dividida: à esquerda o painel antigo (título e botão), à direita o novo (indicadores e gráficos); o mesmo par para a recomendação de bundle e a gestão de usuários. Depois o trecho da especificação de interface listando os componentes declarados. Rótulo: "A ESPECIFICAÇÃO ESTAVA CERTA · O GERADOR JOGAVA FORA".
**TELA:** 630-app-tela-dashboard-vigilancia.png, 885-app-form-dashboard-vigilancia-resposta.png, 628-app-tela-recomendacao-bundle.png, 881-app-form-recomendacao-bundle-resposta.png, 631-app-tela-gestao-de-usuarios.png, 887-app-form-gestao-de-usuarios-resposta.png
**NOTA PRODUÇÃO:** correção no gerador em cfbd72d (a tela renderiza todos os tipos declarados), 3b85623 (sem repetição do que a tela já mostra), ed5b5e7, 8097313 e 1f20b67 (leitura do dado pelo nome certo, sem metadado, com as marcações refletindo a resposta). Documento com a conferência tela a tela: REQUISITO-x-REALIDADE-biobyte.pdf.

## Cena 25 — Todos os agentes ao vivo: o executor da Rede de Petri dentro do sistema
**NARRAÇÃO:** E para fechar, a visão de quem opera o sistema por dentro: o executor da Rede de Petri, embutido no próprio aplicativo, em "Admin / Petri". Aqui está a rede que desenhamos lá na etapa de modelagem — quinze lugares, dezessete transições, onze agentes — só que agora executando de verdade. A entrada do fluxo já vem preenchida com o contexto corrente: quem entrou, o caso aberto, as variáveis clínicas. Clico em "Executar tudo" e a rede anda sozinha: cada transição dispara a tarefa do seu agente, e cada lugar recebe o resultado e o mescla ao contexto para o próximo — autenticação, importação, microbiologia pelo laboratório externo, classificação, alerta, escore de Cox, bundle, estimativa, painel, auditoria, relatório, orquestração — até o token chegar ao "Fim do Fluxo". Na aba Logs, os cinquenta e oito eventos com o horário de cada disparo; na aba Outputs, o que cada lugar entregou — é o mesmo encadeamento que a Rede de Petri prescreve, rodando no código gerado. Do requisito à rede, da rede ao código, do código ao sistema executando com os agentes trabalhando.
**PRODUÇÃO:** aba Execução com a "📥 Entrada do fluxo (JSON)" preenchida (contexto corrente); clicar "⏩ Executar tudo"; mostrar o painel andando (Step/Eventos/Habilitadas), as transições disparando e o token chegando a P_fim (3 tokens). Depois aba Logs (Eventos 58, sequência T_start → authenticate → init_import → fetch_microbiology → classify → mdr → cox → bundle → estimate → dashboard → …) e aba Outputs (P1/P2 com o contexto completo e status completed; P3 com id_amostra/microrganismo). Rótulo: "A REDE DE PETRI EXECUTANDO O SISTEMA".
**TELA:** 261-app-petri-executor.png, 262-app-petri-entrada-do-fluxo.png, 263-app-petri-exec-15s.png, 264-app-petri-exec-final.png, 265-app-petri-aba-logs.png, 266-app-petri-aba-outputs.png, 267-app-petri-aba-inputs.png, 268-app-petri-aba-operacao.png
**NOTA PRODUÇÃO:** v14 (sessão 3618991d): o executor gerado ganhou o campo "Entrada do fluxo" (template 5ac1b2d) — antes rodava a cadeia vazia (o motor lê a entrada de cada lugar em `input_data`). Execução completa em ~47 s (agentes DeepSeek v4-flash sem raciocínio). A aba Operação permite disparar uma tarefa manualmente com JSON (bom para "fazer uma pergunta" a um agente específico no vídeo).

## Cena 26 — Etapa 11: Implantação (o sistema sai do papel e vai para o ar)
**NARRAÇÃO:** Falta a penúltima etapa: implantar. Aqui a ferramenta pega uma versão da Geração de Código — a lista mostra todas, com data e número de arquivos — e sobe o sistema de verdade. Antes de implantar, a configuração: onde fica o banco, qual o provedor de inteligência artificial, a chave. Repare num detalhe importante de segurança: o pacote gerado traz o modelo de configuração, mas nenhum segredo — nem senha de banco, nem chave de API. Isso é informado aqui, na hora de implantar, por quem tem a credencial. Clico em "Implantar esta versão". A ferramenta escreve os oitenta e cinco arquivos, cria um ambiente isolado só para esta implantação, instala o que falta e sobe três serviços: o servidor de agentes, a API do sistema e a interface. E mostra exatamente onde cada um ficou disponível, com o endereço clicável — servidor de agentes na porta cinco mil e dois, API na oito mil e um, interface na três mil e dois. As portas são escolhidas livres, então implantar não derruba nada do que já estava rodando. O registro da implantação mostra cada passo, e o histórico guarda todas as implantações do projeto, com estado e duração.
**PRODUÇÃO:** página "Implantação". Percorrer: lista de versões geradas (destacar a mais recente) → formulário "Configuração da implantação" sendo preenchido (banco `biobyte_app`, usuário, senha mascarada, provedor `deepseek`) → destacar a frase "o pacote gerado traz o modelo, mas nenhum segredo" → clique em "🚀 Implantar esta versão" → estado "preparando/instalando" → estado **NO AR** com a tabela "Onde o sistema ficou disponível" (3 serviços, portas e links) → registro da implantação rolando → histórico. Rótulo: "IMPLANTAÇÃO REAL · AMBIENTE ISOLADO · PORTAS LIVRES".
**TELA:** 500-langnet-implantacao-inicial.png, 501-langnet-implantacao-versao-escolhida.png, 502-langnet-implantacao-configuracao.png, 503-langnet-implantacao-em-andamento.png, 504-langnet-implantacao-no-ar.png, 505-langnet-implantacao-registro.png, 506-langnet-implantacao-historico.png
**NOTA PRODUÇÃO:** implantação real da sessão `3e81772f` pelo motor do LangNet. Correções que esta etapa exigiu (commits a7d1dd8, 3960e6a): o motor procurava o ambiente Python num caminho fixo (quebrava em toda máquina com outro conda); instalava as dependências do app no ambiente COMPARTILHADO e chegou a quebrar o FastAPI do próprio LangNet — agora cada implantação tem ambiente próprio; e usava portas fixas, colidindo com a interface do LangNet — agora aloca portas livres e injeta os endereços reais na interface do sistema.

## Cena 27 — Etapa 12: Monitoramento (o que os agentes fizeram, de verdade)
**NARRAÇÃO:** E a última etapa: monitorar o que está no ar. Esta tela lê o registro ao vivo da implantação — nada aqui é simulado. Veja os números do sistema implantado depois de processar um caso clínico completo: treze tarefas exercitadas, treze execuções com sucesso, nenhuma falha, sete vírgula um segundos de duração média, e três chamadas a ferramentas externas pelo protocolo MCP. Embaixo, uma linha por tarefa: qual agente a executou, se ela roda como procedimento fixo ou como agente de inteligência artificial, quantas vezes rodou, quanto demorou na última vez e qual foi o último erro, se houve. Repare que dá para ver a natureza de cada tarefa — a classificação NHSN e a recomendação de bundle rodam como agente, porque exigem julgamento; a importação e o cálculo do escore rodam como procedimento fixo, porque são exatos e auditáveis. Na aba de ferramentas externas, as chamadas ao laboratório e ao serviço de escore, com os argumentos enviados e os campos recebidos. E o registro ao vivo, filtrável — aqui filtrado pelos eventos de tarefa. Do requisito ao código, do código ao sistema no ar, e do sistema no ar de volta para a tela: o ciclo inteiro, rastreável.
**PRODUÇÃO:** página "Monitoramento". Abrir com os seis indicadores (13 tarefas · 13 sucessos · 0 falhas · 7.1s · 3 chamadas MCP · 1192 linhas). Descer para a tabela por tarefa, pausando nas etiquetas "agente" (classify_case_nhsn, recommend_treatment_bundle, estimate_risk_reduction) versus "determinístico". Trocar para a aba "Ferramentas externas" (consultar_microbiologia e escore_risco_cox com argumentos e campos). Fechar na aba "Registro ao vivo" com o filtro `[task]` aplicado, mostrando a sequência INICIO/OK por tarefa. Rótulo: "MONITORAMENTO REAL · LIDO DA IMPLANTAÇÃO".
**TELA:** 507-langnet-monitoramento-tarefas.png, 508-langnet-monitoramento-ferramentas-externas.png, 509-langnet-monitoramento-registro.png, 510-langnet-monitoramento-registro-filtrado.png
**NOTA PRODUÇÃO:** os números vêm do sistema implantado processando a cadeia clínica (12/13 — a 13ª é a administração de usuário, que precisa dos dados do usuário). Para o Monitoramento existir foi preciso dar OBSERVABILIDADE ao servidor de agentes gerado (commit d3d6262): os eventos de tarefa só iam para o cliente pela conexão e nada ficava no registro do processo — agora cada tarefa escreve `[task] INICIO/OK/ERRO` com a duração, e é isso que a tela lê.


## Cena 28 — Validação a partir dos casos de teste: o que o sistema faz, conferido um a um
**NARRAÇÃO:** Falta a parte que ninguém gosta de mostrar: o que não funcionava. A etapa de Casos de Teste gerou quarenta e três casos de causa e efeito, a partir dos casos de uso. Nós os executamos contra o sistema implantado, um a um. Na primeira rodada, o placar foi duro: apenas três comportamentos corretos, sete defeitos confirmados, e sete elementos que a especificação manda exibir simplesmente não existiam na tela. A exportação de relatórios devolvia os dados e nenhum arquivo. A consulta de auditoria respondia vazio mesmo com registros. Sem os dados clínicos obrigatórios, o sistema calculava o escore assim mesmo — e, pior, o agente inventava um protocolo de tratamento em vez de dizer que faltava informação. Em domínio clínico, responder sem dado é pior do que recusar. Cada um desses defeitos foi corrigido no gerador, nunca no aplicativo, e provado do jeito mais difícil: regenerando o sistema inteiro pela interface do LangNet e implantando de novo, a cada correção. Sete rodadas de regeneração e implantação. O placar final: nove comportamentos corretos, nenhuma falha, e os vinte e cinco elementos de interface presentes. O sistema agora recusa quando falta dado, avisa quando o laboratório externo devolve resultado fora do padrão, distingue "nenhum registro" de "erro", e fala com o usuário nas palavras do caso de uso — "Credenciais inválidas", e não "verificação de pós-condição falhou". Seis casos continuam não exercitáveis, porque exigem provocar falhas internas, e cinco efeitos não estão implementados: e-mail, notificação por push, tempo esgotado, processamento assíncrono e o carregamento animado. Isso está escrito no relatório, com todas as letras.
**PRODUÇÃO:** abrir com o placar em duas colunas — ANTES (3 corretos · 7 defeitos · 18 de 25 elementos) e DEPOIS (9 corretos · 0 defeitos · 25 de 25 elementos). Passar pelas provas na tela do sistema: login com senha errada mostrando "Credenciais inválidas"; a classificação com o selo; o escore com a barra de progresso mostrando vinte e quatro por cento e nível de risco Baixo; a auditoria dizendo "Nenhum registro encontrado" com o botão "Exportar CSV"; o relatório com o arquivo gerado. Fechar no terminal com a linha final da bateria: "comportamento — PASSOU: 9 | FALHOU: 0". Rótulo: "CORRIGIDO NO GERADOR · REGERADO E IMPLANTADO PELA INTERFACE".
**TELA:** 650-validacao-login-credenciais-invalidas-antes.png, 651-validacao-login-credenciais-invalidas.png, 673-validacao-classificacao-selo-antes.png, 674-validacao-classificacao-selo.png, 675-validacao-escore-barra-progresso-antes.png, 676-validacao-escore-barra-progresso.png, 677-validacao-auditoria-nenhum-registro-antes.png, 678-validacao-auditoria-nenhum-registro.png, 679-validacao-relatorio-arquivo-gerado-antes.png, 680-validacao-relatorio-arquivo-gerado.png, 672-langnet-implantacao-historico.png, 617-langnet-monitoramento-tarefas.png
**NOTA PRODUÇÃO:** bateria em docs/apresentacao/demo-biobyte/e2e/run_test_cases.py (46 verificações: 43 casos + 1 sonda de recusa em duas tarefas + 1 caso de uso sem casos gerados). Correções de gerador desta rodada: 189a8ad (consulta multi-coluna vira lista de registros; a recobrança do contrato deixa de induzir invenção), 5b701b0 (o vocabulário do caso de uso chega ao app: selo, barra, exportação e mensagens), c07b427 (mesmo desfecho nas telas de agente), 6bc468c (captura de lista só para nome de coleção), 1504e9b (consulta sem resultado para a tarefa em vez de gravar nulo). 3309121 (corrige a recursão do renderizador de desfecho, que derrubava a aba ao exibir resultado — achado dirigindo o app pela interface). Sessões geradas pela interface nesta rodada: 332b0907, 58a867b8, d6b52738, 7b2e9e88, 388013c4, 64268e56 (final, portas 5015/8009/3011).

## Cena 29 — O relatório de validação: o que ficou de fora, dito na cara
**NARRAÇÃO:** Uma ferramenta que só mostra o que deu certo não serve para decidir nada. Por isso o relatório de validação lista, junto com o que passou, o que não passou e o que nem foi possível testar. Cinco efeitos não estão implementados no sistema gerado: envio de e-mail e notificação por push, tratamento de tempo esgotado, processamento assíncrono de relatório e o carregamento animado do painel. Seis casos não são exercitáveis por fora, porque exigem provocar uma falha interna do próprio sistema. E um caso de uso inteiro — a gestão de usuários e permissões — ficou sem casos de teste, porque a etapa que gera os casos falhou justamente ali. Nada disso está escondido: está no relatório, com o motivo de cada um. É essa honestidade que torna o placar dos nove comportamentos corretos digno de confiança.
**PRODUÇÃO:** documento VALIDACAO-SISTEMA-biobyte.pdf aberto na tabela de resultados, rolando até a seção do que não foi implementado. Rótulo: "O QUE NÃO FUNCIONA TAMBÉM ESTÁ NO RELATÓRIO".
**TELA:** (documento) docs/apresentacao/demo-biobyte/VALIDACAO-SISTEMA-biobyte.pdf
